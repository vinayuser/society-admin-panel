import React from 'react';
import { Card, CardBody } from 'reactstrap';

const Settings = () => (
  <div>
    <div className="page-header">
      <h1>Settings</h1>
    </div>
    <Card>
      <CardBody>
        <p className="text-muted mb-0">Platform settings — coming soon.</p>
      </CardBody>
    </Card>
  </div>
);

export default Settings;
